import styled from 'styled-components';

export const Wrapper = styled.div`
    height: calc(100% - 4rem);
    padding: 0 1rem;
    overflow-y: auto;

    & ul {
        padding-bottom: 3rem;
    }
`;